#include <stdio.h>
#include <stdlib.h>

int main() {
    int i, vet[10];

    printf("\nInforme 10 elementos inteiros:");

    for(int i=0;i<10;i++) {
        printf("\nDigite o elemento %d: ", i+1);
        scanf
    }
}